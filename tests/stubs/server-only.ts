// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component. Aliased in vitest.config.ts so the data facade and its
// source adapters can be imported and unit-tested under Node.
export {};
